# NEARWEEK MCP Server Configuration

# Network selection (mainnet or testnet)
NEAR_NETWORK=mainnet

# Custom RPC URL (optional - overrides default network endpoint)
# NEAR_RPC_URL=https://rpc.mainnet.fastnear.com
# RPC_URL=https://rpc.mainnet.fastnear.com

# API Key for authenticated RPC providers (optional)
# Sent as Authorization: Bearer <key> header
# NEAR_API_KEY=your-api-key-here
# API_KEY=your-api-key-here

# HTTP server port (only for HTTP transport mode)
PORT=3000
